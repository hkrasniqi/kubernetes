apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: www
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi
  storageClassName: "hostpath"